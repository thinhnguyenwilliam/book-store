name: bookstore

# File gốc chỉ khai báo tài nguyên dùng chung. Các service được chia theo nhóm
# trong compose/ và được Makefile ghép lại theo đúng thứ tự.
networks:
  backend:

volumes:
  postgres-data:
  pgadmin-data:
  redis-data:
  redisinsight-data:
  rabbitmq-data:
  kafka-data:
  elasticsearch-data:
  app-logs:
  otel-collector-data:
  otel-log-consumer-data:
  tempo-data:
  prometheus-data:
  loki-data:
  grafana-data:
